/**
 * Generates an error message for an invalid hexadecimal color string.
 * @param hex - The invalid hexadecimal color string.
 * @returns An error message string describing the invalid hex value.
 */
export const invalidHexError = (hex: string) => `Invalid hex string ${hex}`;
